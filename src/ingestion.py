from utils.main import read_yaml
import argparse
def get_data(configPathForRemote):

if __name__=='__main__':
    args=argparse.ArgumentParser().add_argument('--config','-c',default='config/config.yaml')
    print(args.config)